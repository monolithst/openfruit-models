const {
  locationInfo,
} = require('../models/geo')


const fileLocator = ({filePath}) => {
  const getLocationInfo = ({lat, lon}) => {
    return {
    }
  }

  return {
    getLocationInfo
  }
}


module.exports = fileLocator
